// Behavioral spec for the feature-catalog HTTP surface. Shapes live in
// `@dod/api-contract` (lab); DOD-0030 builds the realm that turns these green.

describe('FeatureGate (api)', () => {
  describe('GET /v1/feature', () => {
    it.todo('returns the feature catalog for a Universe');
    it.todo('400 when universeId is missing or does not resolve');
  });
});
