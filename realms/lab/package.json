{
  "name": "lab",
  "version": "0.0.1",
  "description": "Simulation realm — drives the engine to produce balance signal",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "test": "pnpm run test:api",
    "test:api": "jest --config ./test/jest-api.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@dod/config": "workspace:*",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.6.2",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.9",
    "typescript": "^6.0.3"
  }
}
